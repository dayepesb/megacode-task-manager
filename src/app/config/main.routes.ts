import {Routes} from '@angular/router';
import {PrincipalPageComponent} from '@app/general/principal-page/principal-page.component';

export const APP_ROUTES: Routes = [
  {path: '', component: PrincipalPageComponent, children: [

    ]}
]
